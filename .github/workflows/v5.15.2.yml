name: Build Qt lite

on:
  pull_request:
    branches:
      - 'master'
  push:
    branches:
      - 'master'
  #schedule:
  #  - cron: "0 13 * * 1"
    # https://jasonet.co/posts/scheduled-actions/

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-20.04
        ocaml-version:
          - 4.08.1

    runs-on: ${{ matrix.os }}

    steps:
      - name: Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.7.0
        with:
          access_token: ${{ github.token }}

      - name: Checkout code
        uses: actions/checkout@v2

      - name: Retrieve date for cache key
        id: cache-key
        run: echo "::set-output name=date::$(/bin/date -u "+%Y%m%d")"
        shell: bash

      - name: Use OCaml ${{ matrix.ocaml-version }}
        uses: avsm/setup-ocaml@v1
        with:
          ocaml-version: ${{ matrix.ocaml-version }}

      #- run: sudo apt install opam ocaml-nox
      #- run: opam init --yes

      #- run: sudo apt install default-libmysqlclient-dev dh-exec firebird-dev firebird3.0-common firebird3.0-common-doc \
      #          freetds-dev gir1.2-harfbuzz-0.0 libasound2-dev libatk-bridge2.0-dev libatk1.0-dev \
      #          libatspi2.0-dev libcairo-script-interpreter2 libcairo2-dev libct4 libcups2-dev libcupsimage2-dev \
      #          libdatrie-dev libdbus-1-dev libdouble-conversion-dev libevdev-dev libfbclient2 \
      #          libfribidi-dev libgbm-dev libgdk-pixbuf2.0-dev libgraphite2-dev libgtk-3-dev libgudev-1.0-dev  \
      #          libharfbuzz-dev libharfbuzz-gobject0 libib-util libinput-dev libjbig-dev libmtdev-dev \
      #          libmysqlclient-dev libpango1.0-dev libpixman-1-dev libpq-dev libproxy-dev libthai-dev libtiff-dev \
      #          libtiffxx5 libwacom-dev libxcb-glx0-dev libxcb-icccm4-dev libxcb-render-util0-dev \
      #          libxcb-sync-dev libxcb-xinerama0-dev libxcb-xinput-dev libxcb-xkb-dev libxcomposite-dev libxdamage-dev \
      #          libxft-dev libxkbcommon-x11-dev odbcinst odbcinst1debian2 pango1.0-tools  unixodbc-dev wayland-protocol
      - run: sudo apt install libopengl-dev libxcb-sync-dev libxcb-xinerama0-dev libxcb-xinput-dev libxcb-xkb-dev libxcomposite-dev libxdamage-dev libxft-dev libxkbcommon-x11-dev
      - run: opam pin add . --no-action
      - run: opam depext  Qt5 --yes --with-test
      #- run: opam install Qt5 --deps-only
      - run: opam install Qt5 --yes --verbose
